import type {Action, Resource} from "@/core/types.ts";

export const RESOURCES: Resource[] = ["ROLE", "USER", "PROJECT", "TICKET"];
export const ACTIONS: Action[] = ["READ", "CREATE", "UPDATE", "DELETE"];